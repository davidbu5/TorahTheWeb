{
  "name": "torah-the-web",
  "version": "1.0.0",
  "description": "a tryout for modules",
  "scripts": {
    "prewatch": "node change-server-path && assets-to-unpacked-dist.bat && assets-to-dist.bat",
    "prebuild": "npm i && node change-server-path && assets-to-unpacked-dist.bat",
    "watch": "webpack --config webpack.config.js --watch --mode development",
    "build": "webpack --config webpack.config.js",
    "postbuild": "chrome-pack.bat && assets-to-dist.bat"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/chrome": "0.0.59",
    "@types/node": "^10.17.27",
    "ts-loader": "^4.5.0",
    "typescript": "^2.9.2",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
